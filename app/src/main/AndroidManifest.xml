<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.whatstheweatherapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".WhatsWeatherApplication"
        android:icon="@mipmap/weather_app_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/weather_app_launcher_round"
        android:theme="@style/Theme.WhatsTheWeatherApp"
        tools:targetApi="31">


        <activity
            android:name=".presentation.WeatherForecastLandingActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>